import { View, Text } from 'react-native';
import React from 'react';

const Metrics = () => {
    return (
        <View>
            <Text>Metrics</Text>
        </View>
    );
};

export default Metrics;
